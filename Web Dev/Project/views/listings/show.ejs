<% layout("/layouts/boilerplate") -%>

<body>
    
    <div class="row">
        <div class="col-8 offset-5">
            <h3>Listing Details</h3>
        </div>
        
    </div>

<div class="row">
    <div class="col-8 offset-2">

    <div class="card">
        <img src="<%= listing.image %>" class="card-img-top show-img" alt="listing-image">
        <div class="card-body">
            <p class="card-text">
                <b><%= listing.title %></b> <br>
                <%= listing.description%>   <br>
                &#8377; <%= listing.price.toLocaleString("en-IN") %>    <br>
                <%= listing.location%>  <br>
                <%= listing.country %>  <br>
            </p>
        </div>
    </div>

    <!-- <ul>
        <li><%= listing.title %></li>
        <li><%= listing.description%></li>
        <!-- <li><%= listing.image%></li> -->
        <!-- <li>  <%= listing.price.toLocaleString("en-IN")%></li>  -->
        <!-- toLocaleString() method is used to convert a number to a string using a local specific format and '&8377;'' is the unicode for the Indian Rupee symbol. -->
        <!-- <li><%= listing.location %></li> -->
        <!-- <li><%= listing.country%> </li> -->
    <!-- </ul> --> 

</div>
</div>

<div class="row mt-3 mb-4">
    <div class="col-1 offset-2" >
        <button class="btn btn-danger"><a href="/listings/<%=listing._id%>/edit" class="show-btn">Edit</a></button>
    </div>

    <div class="col-1">
        <form action="/listings/<%=listing._id %>?_method=DELETE " method="POST">
            <button class="btn btn-dark">Delete</button>
        </form>
    </div>
</div>
</body>
